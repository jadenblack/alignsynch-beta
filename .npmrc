/*
  Disable the frozen-lockfile requirement during CI builds.
  This lets pnpm regenerate pnpm-lock.yaml automatically
  when package.json changes, instead of failing the build.
*/
frozen-lockfile=false
prefer-frozen-lockfile=false
